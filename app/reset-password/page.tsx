'use client';import {useState} from 'react';import Link from 'next/link';import {createClient} from '@/lib/supabase/client';
export default function ResetPassword(){const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(p1.length<8)return setMsg('Use at least 8 characters.');if(p1!==p2)return setMsg('Passwords do not match.');setBusy(true);const s=createClient();const{error}=await s.auth.updateUser({password:p1});setMsg(error?error.message:'Password updated. You can now log in with your new password.');setBusy(false)}return <main className="form"><div className="kicker">ACCOUNT RECOVERY</div><h1>Choose a new password</h1>{msg&&<div className="notice">{msg}</div>}<form onSubmit={submit}><div className="field"><label>New password</label><input type="password" minLength={8} value={p1} onChange={e=>setP1(e.target.value)} required/></div><div className="field"><label>Confirm password</label><input type="password" minLength={8} value={p2} onChange={e=>setP2(e.target.value)} required/></div><button className="btn primary" disabled={busy}>{busy?'Updating…':'Update Password'}</button></form><p><Link href="/login">Return to login</Link></p></main>}
