'use client';
import Link from 'next/link';import {useSearchParams,useRouter} from 'next/navigation';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Signup(){const q=useSearchParams();const router=useRouter();const[role,setRole]=useState(q.get('role')||'driver');const[name,setName]=useState('');const[company,setCompany]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();setMsg('');const s=createClient();const{data,error}=await s.auth.signUp({email,password,options:{data:{full_name:name,company_name:company,role}}});if(error)return setMsg(error.message);if(data.user){await s.from('profiles').upsert({id:data.user.id,full_name:name,company_name:company,role,status:'pending'})}setMsg('Account created. Check your email if confirmation is enabled.');setTimeout(()=>router.push('/dashboard'),700)}return <main className="form"><div className="brand" style={{color:'#071a2f'}}>HIGHWAY AUTOMATION</div><h1>Create account</h1>{msg&&<div className="notice">{msg}</div>}<form onSubmit={go}><div className="two"><div className="field"><label>Full name</label><input value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field"><label>Company</label><input value={company} onChange={e=>setCompany(e.target.value)}/></div></div><div className="field"><label>Account type</label><select value={role} onChange={e=>setRole(e.target.value)}><option value="driver">Driver / Carrier</option><option value="shipper">Shipper</option></select></div><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={6} required/></div><button className="btn primary">Create Account</button></form><p className="muted">Already registered? <Link href="/login">Log in</Link></p></main>}
