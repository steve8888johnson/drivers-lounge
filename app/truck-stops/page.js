import PageShell from '../../components/PageShell';import {truckStops} from '../../lib/road-data';
export default function Page(){return <PageShell eyebrow="TRUCK STOPS" title="Find the stop that fits the trip" intro="Compare parking, diesel, DEF, showers, food, CAT Scale, dog areas and RV support."><div className="intel-card-grid">{truckStops.map(s=><article className="intel-card" key={s.id}><div className="intel-card-top"><b>{s.name}</b><span>★ {s.rating}</span></div><p>{s.city} · {s.parking} parking spaces</p><div className="amenities">{[s.diesel&&'Diesel',s.def&&'DEF',s.cat&&'CAT Scale',s.showers&&'Showers',s.dogPark&&'Dog park',s.rv&&'RV'].filter(Boolean).map(x=><span key={x}>{x}</span>)}</div><small>{s.food.join(' · ')}</small></article>)}</div></PageShell>}
