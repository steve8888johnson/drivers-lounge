'use client';import Link from 'next/link';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function ForgotPassword(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const s=createClient();const redirectTo=`${window.location.origin}/reset-password`;const{error}=await s.auth.resetPasswordForEmail(email,{redirectTo});setMsg(error?error.message:'If that email is registered, a password reset link has been sent.');setBusy(false)}return <main className="form"><div className="kicker">ACCOUNT RECOVERY</div><h1>Reset your password</h1><p className="muted">Enter the email used for your Highway Automation account.</p>{msg&&<div className="notice">{msg}</div>}<form onSubmit={submit}><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><button className="btn primary" disabled={busy}>{busy?'Sending…':'Send Reset Link'}</button></form><p><Link href="/login">Back to login</Link></p></main>}
