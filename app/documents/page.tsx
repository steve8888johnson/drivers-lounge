'use client';import {useEffect,useState,useMemo} from 'react';import {createClient} from '@/lib/supabase/client';
const types=['CDL','Insurance','W9','MC Authority','Medical Card','NOA','BOL','POD','Rate Confirmation'];
export default function Documents(){const[docs,setDocs]=useState<any[]>([]),[msg,setMsg]=useState('');const s=useMemo(()=>createClient(),[]);async function refresh(){const{data:{user}}=await s.auth.getUser();if(!user)return;const{data}=await s.from('ha_documents').select('*').eq('owner_id',user.id).order('created_at',{ascending:false});setDocs(data||[])}useEffect(()=>{void refresh()},[]);async function upload(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');const f=new FormData(e.currentTarget),file=f.get('file') as File,type=String(f.get('type'));const{data:{user}}=await s.auth.getUser();if(!user)return;const path=`${user.id}/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const{error:up}=await s.storage.from('ha-documents').upload(path,file);if(up)return setMsg(up.message);const{error}=await s.from('ha_documents').insert({owner_id:user.id,document_type:type,file_name:file.name,file_url:path,status:'pending'});if(error)return setMsg(error.message);setMsg('Uploaded for verification.');(e.currentTarget as HTMLFormElement).reset();await refresh()}return <main className="wrap section"><div className="kicker">SECURE DOCUMENT CENTER</div><h1>Documents</h1><p className="muted">Upload carrier credentials and load paperwork. Documents remain private and are reviewed by Highway Automation.</p>{msg&&<div className="notice">{msg}</div>}<form className="card" onSubmit={upload}><div className="two"><div className="field"><label>Document type</label><select name="type">{types.map(t=><option key={t}>{t}</option>)}</select></div><div className="field"><label>File</label><input name="file" type="file" required/></div></div><button className="btn primary">Upload Document</button></form><h2 style={{marginTop:28}}>Your uploads</h2><table className="table"><thead><tr><th>Type</th><th>File</th><th>Status</th><th>Uploaded</th></tr></thead><tbody>{docs.map(d=><tr key={d.id}><td>{d.document_type}</td><td>{d.file_name}</td><td><span className="pill">{d.status}</span></td><td>{new Date(d.created_at).toLocaleDateString()}</td></tr>)}{!docs.length&&<tr><td colSpan={4} className="empty">No documents uploaded yet.</td></tr>}</tbody></table></main>}
