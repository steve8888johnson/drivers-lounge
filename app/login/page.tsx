'use client';
import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg('');const s=createClient();const{error}=await s.auth.signInWithPassword({email,password});if(error){setMsg(error.message);setBusy(false);return}router.push('/dashboard');router.refresh()}return <main className="form"><div className="brand" style={{color:'#071a2f'}}>HIGHWAY AUTOMATION</div><h1>Log in</h1>{msg&&<div className="notice">{msg}</div>}<form onSubmit={go}><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></div><button className="btn primary" type="submit" disabled={busy}>{busy?'Logging in…':'Log In'}</button></form><p><Link href="/forgot-password">Forgot your password?</Link></p><p className="muted">Need an account? <Link href="/signup">Create one</Link></p></main>}
