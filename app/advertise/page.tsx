import Link from 'next/link';
const plans=[['Starter','299','30 days','Load board inline placement'],['Growth','599','30 days','Top load board + inline rotation'],['Premium','999','30 days','Top placement + pop-up rotation + priority']];
export default function Advertise(){return <><header className="nav"><div className="wrap navin"><Link className="brand" href="/">HIGHWAY AUTOMATION<span>ADVERTISING</span></Link><div className="links"><Link href="/loadboard">Load Board</Link></div></div></header><main className="wrap section"><div className="kicker">REACH TRUCKING DECISION MAKERS</div><h1>Advertise on Highway Automation</h1><p className="muted">Reach drivers, carriers and shippers while they are actively finding freight and managing loads.</p><div className="grid3" style={{marginTop:24}}>{plans.map(([name,price,term,desc])=><div className="card" key={name}><div className="kicker">{name}</div><h2>${price}</h2><p className="muted">{term}</p><p>{desc}</p><Link className="btn primary" href={`/advertise/new?plan=${name.toLowerCase()}`}>Start Campaign</Link></div>)}</div><div className="card" style={{marginTop:22}}><h2>High-value categories</h2><p className="muted">Factoring, insurance, fuel cards, tires, truck repair, truck stops, attorneys, permits, bookkeeping, ELDs, financing, recruiting and equipment sales.</p></div></main></>}
