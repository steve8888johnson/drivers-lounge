'use client';import {useState} from 'react';import {useSearchParams} from 'next/navigation';import {createClient} from '@/lib/supabase/client';
export default function NewCampaign(){const plan=useSearchParams().get('plan')||'starter';const[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),s=createClient();const prices:any={starter:299,growth:599,premium:999};const placement=plan==='premium'?'popup':plan==='growth'?'loadboard_top':'loadboard_inline';const{error}=await s.from('advertising_campaigns').insert({advertiser_name:f.get('advertiser_name'),headline:f.get('headline'),body:f.get('body'),cta_text:f.get('cta_text')||'Learn More',cta_url:f.get('cta_url'),placement,billing_model:'flat',price:prices[plan]||299,active:false});setMsg(error?error.message:'Campaign submitted. We will activate it after payment and review.')}return <main className="form"><div className="kicker">{plan.toUpperCase()} CAMPAIGN</div><h1>Start Advertising</h1>{msg&&<div className="notice">{msg}</div>}<form onSubmit={submit}><div className="field"><label>Business name</label><input name="advertiser_name" required/></div><div className="field"><label>Ad headline</label><input name="headline" required maxLength={90}/></div><div className="field"><label>Ad message</label><textarea name="body" rows={4} required/></div><div className="two"><div className="field"><label>Button text</label><input name="cta_text" placeholder="Get a Quote"/></div><div className="field"><label>Destination URL</label><input name="cta_url" type="url" placeholder="https://" required/></div></div><button className="btn primary">Submit Campaign</button></form></main>}
