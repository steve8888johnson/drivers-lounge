'use client';
import {useMemo,useState} from 'react';
import {carriers} from '../lib/data';
export default function CarrierSearch(){const [q,setQ]=useState('');const found=useMemo(()=>carriers.filter(c=>`${c.name} ${c.usdot} ${c.mc} ${c.state}`.toLowerCase().includes(q.toLowerCase())),[q]);return <div><div className="searchbox"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search carrier name, USDOT or MC number"/><button>Search</button></div><div className="cards">{found.map(c=><article className="data-card" key={c.usdot}><div className="card-line"><div><small>USDOT {c.usdot} · {c.mc}</small><h3>{c.name}</h3></div><b className="rating">★ {c.rating}</b></div><p>{c.state} · {c.status} · Safety rating: {c.safety}</p><div className="metric-grid"><span><b>{c.powerUnits}</b> power units</span><span><b>{c.drivers}</b> drivers</span><span><b>{c.pay}</b> pay accuracy</span><span><b>{c.equipment}</b> equipment</span></div><button>View carrier profile</button></article>)}</div></div>}
