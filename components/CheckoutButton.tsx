'use client';import {useState} from 'react';
export default function CheckoutButton({product,label,campaignId}:{product:string;label:string;campaignId?:string}){const[busy,setBusy]=useState(false);async function go(){setBusy(true);const r=await fetch('/api/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({product,campaign_id:campaignId})});const d=await r.json();if(d.url)location.href=d.url;else{alert(d.error||'Checkout unavailable');setBusy(false)}}return <button className="btn primary" onClick={go} disabled={busy}>{busy?'Opening secure checkout…':label}</button>}
