(function(){const{$,notice}=window.DLUI;const fields=['customer','pickup','delivery','rate','equipment','notes'],key='dl-v8-dispatch';try{const saved=JSON.parse(localStorage.getItem(key)||'null');if(saved){for(const f of fields)$('#dispatch-'+f).value=saved[f]||'';notice('Loaded your trip note from this device.')}}catch(_){notice('The previous device note could not be read. You can enter a new note.',true)}const record=()=>Object.fromEntries(fields.map(f=>[f,$('#dispatch-'+f).value.trim()]));$('#dispatch-form').addEventListener('submit',e=>{e.preventDefault();try{localStorage.setItem(key,JSON.stringify({...record(),time:Date.now()}));notice('Trip note saved on this device. No messages have been sent.')}catch(_){notice('This browser could not save your note. Use Export to keep a copy.',true)}});$('#dispatch-export').onclick=()=>{const blob=new Blob([JSON.stringify(record(),null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download='drivers-lounge-trip.json';link.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}})();
