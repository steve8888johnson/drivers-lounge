
(function(){
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
function toast(msg){let t=$('.toast-global');if(!t){t=document.createElement('div');t.className='toast-global';document.body.appendChild(t)}t.textContent=msg;t.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.hidden=true,2600)}
$$('[data-filter]').forEach(b=>b.addEventListener('click',()=>{$$('[data-filter]').forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;$$('[data-category]').forEach(x=>x.hidden=f!=='all'&&x.dataset.category!==f)}));
$$('[data-save]').forEach(b=>b.addEventListener('click',()=>{b.textContent='Saved ✓';localStorage.setItem('dl-save-'+b.dataset.save,'1');toast('Saved to your dashboard')}));
$$('[data-apply]').forEach(b=>b.addEventListener('click',()=>{b.textContent='Application started';b.disabled=true;toast('Added to Driver Passport applications')}));
const reportOpen=$('[data-open-report]'), modal=$('#report-modal'); if(reportOpen&&modal){reportOpen.addEventListener('click',()=>modal.hidden=false);$('#close-report').addEventListener('click',()=>modal.hidden=true);$$('[data-report-kind]').forEach(b=>b.addEventListener('click',()=>{localStorage.setItem('dl-last-report',JSON.stringify({kind:b.dataset.reportKind,time:Date.now()}));modal.hidden=true;toast(`${b.textContent.trim()} report saved`)}))}
const aiBtn=$('#ask-ai'); if(aiBtn){aiBtn.addEventListener('click',()=>{const q=$('#ai-question').value.trim(),out=$('#ai-answer');if(!q){toast('Ask the copilot a question');return}let answer='';const s=q.toLowerCase();if(s.includes('parking'))answer='Based on the current demo corridor, stop before Ardmore. Love’s shows 23 reported spaces and historically fills by 8 PM.';else if(s.includes('fuel'))answer='Fuel in Gainesville. The route-adjusted demo price is $3.49, approximately 18¢ below the next major stop.';else if(s.includes('scale'))answer='The Gainesville I-35 inspection station is reported likely open with 90% confidence. Prepare paperwork and follow all posted instructions.';else if(s.includes('weather')||s.includes('wind'))answer='Crosswinds are shown near Ardmore with gusts up to 35 mph. Reduce speed and watch high-profile vehicle stability.';else answer='I would keep the major commercial corridor, verify official restrictions, and plan fuel, parking and inspection stops before departure.';out.textContent=answer;localStorage.setItem('dl-ai-last',JSON.stringify({q,answer}));})}
const search=$('[data-search]');if(search){search.addEventListener('input',()=>{const q=search.value.toLowerCase();$$('[data-search-item]').forEach(x=>x.hidden=!x.textContent.toLowerCase().includes(q))})}
$$('[data-counter]').forEach(el=>{const end=+el.dataset.counter;let v=Math.floor(end*.9);const id=setInterval(()=>{v=Math.min(end,v+Math.max(1,Math.ceil((end-v)/7)));el.textContent=v.toLocaleString();if(v>=end)clearInterval(id)},45)});
})();
