(async function(){const B=window.DLBackend,$=s=>document.querySelector(s),status=$('#delete-status'),guest=$('#delete-guest'),form=$('#delete-web-form'),email=$('#delete-email');function setStatus(t,bad=false){status.textContent=t;status.style.borderColor=bad?'#c82835':'#315269'}if(!B?.configured){guest.hidden=false;setStatus('Account services are unavailable on this build.',true);return}const user=await B.user();if(!user){guest.hidden=false;setStatus('Sign in to verify the account you want deleted.');return}guest.hidden=true;form.hidden=false;email.value=user.email||'';setStatus('Signed in. Confirm below to request deletion.');form.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);if(!fd.get('confirm'))return;const btn=form.querySelector('button[type=submit]');btn.disabled=true;btn.textContent='Submitting…';try{await B.upsert('account_deletion_requests',{user_id:user.id,email:user.email||null,reason:String(fd.get('reason')||'').trim()||null,status:'requested'},'user_id');setStatus('Deletion request received. Drivers Lounge will process the request after verification.');form.reset();email.value=user.email||''}catch(err){console.error(err);setStatus(err.message||'Could not submit the deletion request.',true)}finally{btn.disabled=false;btn.textContent='Request permanent deletion'}})})();