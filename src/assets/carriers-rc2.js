(function(){
const B=window.DLBackend;
const $=s=>document.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const criteria=[['overall_experience','Overall experience'],['pay_accuracy','Pay accuracy'],['pay_level','Pay level'],['miles_load_consistency','Miles / load consistency'],['home_time_promises','Home-time promises kept'],['dispatch_quality','Dispatch quality'],['driver_respect','Driver respect'],['equipment_condition','Equipment condition'],['maintenance_response','Maintenance response'],['safety_culture','Safety culture / HOS pressure'],['accessorial_pay','Detention / layover / breakdown pay'],['benefits','Benefits'],['management_communication','Management communication'],['hiring_promises','Hiring promises vs. actual job'],['would_work_again','Would work there again']];
let carriers=[];let selected=null;let timer=null;
function toast(msg){if(window.DLToast)return window.DLToast(msg);const t=document.createElement('div');t.className='dl-global-toast';t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),2600);}
function locationText(c){return [c.physical_city,c.physical_state].filter(Boolean).join(', ')||'Location unavailable';}
function carrierName(c){return c.dba_name||c.legal_name||('USDOT '+c.usdot_number);}
function renderRows(rows){const body=$('#carrier-results');if(!rows.length){body.innerHTML='<tr><td colspan="6"><div class="empty-state">No carriers matched that search.</div></td></tr>';return;}body.innerHTML=rows.map(c=>`<tr><td><strong>${esc(carrierName(c))}</strong><br><small>USDOT ${esc(c.usdot_number)}${c.mc_number?' · MC '+esc(c.mc_number):''}<br>${esc(locationText(c))}</small></td><td>${esc(c.operating_status||'—')}</td><td>${esc(c.power_units??'—')}</td><td>${esc(c.drivers??'—')}</td><td><span class="badge blue" data-score-for="${esc(c.usdot_number)}">No reviews yet</span></td><td><button class="primary-btn" data-review-usdot="${esc(c.usdot_number)}">View / Review</button></td></tr>`).join('');hydrateScores(rows);}
async function hydrateScores(rows){if(!B?.configured||!B.client)return;try{const ids=rows.map(c=>c.usdot_number);const {data,error}=await B.client.from('carrier_review_scores').select('carrier_usdot,review_count,driver_score').in('carrier_usdot',ids);if(error)throw error;const map=new Map((data||[]).map(s=>[String(s.carrier_usdot),s]));rows.forEach(c=>{const s=map.get(String(c.usdot_number));const el=document.querySelector(`[data-score-for="${c.usdot_number}"]`);if(el&&s)el.textContent=`${Number(s.driver_score).toFixed(2)} / 5 · ${s.review_count} reviews`;});}catch(e){console.debug('Review scores unavailable',e);}}
async function queryCarriers(raw=''){try{if(!B?.configured||!B.client){carriers=await B.list('carriers',{limit:100,order:'legal_name'});renderRows(carriers);return;}let q=B.client.from('carriers').select('*').order('legal_name',{ascending:true}).limit(100);const term=String(raw||'').trim();if(term){const safe=term.replace(/[,()]/g,' ').trim();if(/^\d+$/.test(safe))q=q.or(`usdot_number.eq.${safe},mc_number.ilike.%${safe}%,legal_name.ilike.%${safe}%,dba_name.ilike.%${safe}%`);else q=q.or(`legal_name.ilike.%${safe}%,dba_name.ilike.%${safe}%,mc_number.ilike.%${safe}%,physical_city.ilike.%${safe}%,physical_state.ilike.%${safe}%`);}const {data,error}=await q;if(error)throw error;carriers=data||[];renderRows(carriers);}catch(e){console.error(e);renderRows([]);toast('Carrier database needs the RC2 Supabase migration and FMCSA import.');}}
function reviewForm(){return criteria.map(([key,label])=>`<label class="rating-row"><span>${esc(label)}</span><select name="${key}" required><option value="">Rate</option>${[5,4,3,2,1].map(n=>`<option value="${n}">${n} / 5</option>`).join('')}</select></label>`).join('');}
function openCarrier(c){selected=c;$('#carrier-modal-title').textContent=carrierName(c);$('#carrier-modal-sub').textContent=`USDOT ${c.usdot_number} · ${locationText(c)}`;$('#ratings-grid').innerHTML=reviewForm();$('#carrier-modal').hidden=false;loadReviews(c.usdot_number);}
async function loadReviews(usdot){const box=$('#carrier-review-list');box.innerHTML='<p class="page-subtitle">Loading driver reviews…</p>';try{const rows=await B.list('carrier_reviews',{eq:{carrier_usdot:usdot,moderation_status:'published'},limit:50,order:'created_at'});if(!rows.length){box.innerHTML='<p class="page-subtitle">No published driver reviews yet. Be the first.</p>';return;}let responseMap=new Map();try{const responses=await B.list('carrier_responses',{eq:{moderation_status:'published'},limit:100,order:'created_at'});responseMap=new Map(responses.map(x=>[String(x.review_id),x]));}catch{}box.innerHTML=rows.map(r=>{const response=responseMap.get(String(r.id));return `<article class="review-card" data-review-id="${esc(r.id)}"><div><strong>${r.employment_status==='current_driver'?'Current driver':'Former driver'}</strong> <span class="badge ${r.verification_status==='verified'?'green':'blue'}">${esc(r.verification_status)}</span></div><div class="review-score">${Number(r.overall_experience).toFixed(1)} / 5 overall</div><p>${esc(r.review_text||'No written comments.')}</p>${response?`<div class="carrier-response"><strong>Carrier response</strong><p>${esc(response.response_text)}</p></div>`:''}<button class="secondary-btn" data-report-review="${esc(r.id)}" type="button">Report review</button></article>`;}).join('');}catch(e){box.innerHTML='<p class="page-subtitle">Reviews will appear after the RC2 migration is applied.</p>';}}
async function reportReview(id){const user=await B.user();if(!user){toast('Sign in to report a review.');location.href='/account';return;}const reason=prompt('Why are you reporting this review?');if(!reason)return;try{await B.insert('carrier_review_reports',{review_id:id,reporter_user_id:user.id,reason:String(reason).slice(0,500),status:'open'});toast('Review reported for moderation.');}catch(err){toast(err.message||'Could not report review.');}}
async function submitReview(e){e.preventDefault();if(!selected)return;const user=await B.user();if(!user){toast('Sign in before leaving a carrier review.');location.href='/account';return;}const f=new FormData(e.currentTarget);const row={carrier_usdot:Number(selected.usdot_number),reviewer_user_id:user.id,employment_status:f.get('employment_status'),review_text:String(f.get('review_text')||'').trim(),verification_status:'unverified',moderation_status:'published'};for(const [k] of criteria)row[k]=Number(f.get(k));try{await B.upsert('carrier_reviews',row,'carrier_usdot,reviewer_user_id');toast('Your carrier review was saved.');e.currentTarget.reset();loadReviews(selected.usdot_number);hydrateScores([selected]);}catch(err){console.error(err);toast(err.message||'Could not save review.');}}
document.addEventListener('click',e=>{const b=e.target.closest('[data-review-usdot]');if(b){const c=carriers.find(x=>String(x.usdot_number)===b.dataset.reviewUsdot);if(c)openCarrier(c);}const report=e.target.closest('[data-report-review]');if(report)reportReview(report.dataset.reportReview);if(e.target.matches('[data-close-carrier]'))$('#carrier-modal').hidden=true;});
$('#carrier-search')?.addEventListener('input',e=>{clearTimeout(timer);timer=setTimeout(()=>queryCarriers(e.target.value),280);});$('#carrier-search-btn')?.addEventListener('click',()=>queryCarriers($('#carrier-search').value));$('#carrier-review-form')?.addEventListener('submit',submitReview);queryCarriers();
})();